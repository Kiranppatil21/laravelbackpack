Developer shortcuts & links

- Cypress docs (local-run + CI): see `docs/CYPRESS.md`.

Note: I tried to insert a short link into `README.md` pointing to `docs/CYPRESS.md`, but the repository's `README.md` is enclosed in a large fenced block and my automated patch failed. If you'd like, you can add this single line near the end of your `README.md` where other developer notes live:

    - Cypress local-run and CI parallelization instructions: see `docs/CYPRESS.md`.

If you want me to try again to patch `README.md` directly, say so and I will retry (or I can open a small PR with the change).